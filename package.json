{
  "version": "0.0.25",
  "scripts": {
    "dev": "pnpm -r start",
    "build": "pnpm -r build",
    "release": "bumpp package.json --commit --push --tag --all"
  },
  "devDependencies": {
    "bumpp": "9.1.1"
  }
}
